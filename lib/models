class Category {
  final String name;
  Category({required this.name});
  
  // Firebase'den okurken kullanacağız
  factory Category.fromMap(Map<String, dynamic> map) {
    return Category(name: map['name'] ?? '');
  }
}